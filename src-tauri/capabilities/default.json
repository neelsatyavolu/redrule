{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window and the call banner talk to the backend through Minutes' own commands and state events.",
  "windows": ["main", "banner"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
